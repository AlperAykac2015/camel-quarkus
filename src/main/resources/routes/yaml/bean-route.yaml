- route:
    id: yaml-bean-route
    from:
      uri: "platform-http:/yaml/bean"
      steps:
        - setBody:
            constant: "Original request"
        - bean:
            ref: "greetingBean"
            method: "enrich"
